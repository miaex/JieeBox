package com.jiee.box.ui.theme

import androidx.compose.ui.graphics.Color

// Palette pulled from the JIEE BOX app icon artwork: warm cream, dusty blue,
// terracotta and coral — dynamic without being neon/aggressive.
val JieeBlue = Color(0xFF4A78C4)          // primary: box lid color, buttons, links
val JieeBlueDeep = Color(0xFF2F4E86)      // pressed/emphasis states, dark text accents
val JieeTerracotta = Color(0xFFE97142)    // box side color: active-state accent
val JieeTerracottaDeep = Color(0xFFC85A30) // stop button, stronger warm accent
val JieePink = Color(0xFFF98279)          // coral strap accent: secondary highlights

val JieeBackground = Color(0xFFFCEBD6)    // warm cream, matches the icon background
val JieeSurface = Color(0xFFFFF8EE)       // card surfaces, slightly lighter cream
val JieeOutline = Color(0xFFF0D9BE)       // soft warm outline/border

val JieeTextPrimary = Color(0xFF2F3B52)   // deep blue-grey, readable on cream
val JieeTextSecondary = Color(0xFF8A796C) // warm muted brown-grey for secondary text
