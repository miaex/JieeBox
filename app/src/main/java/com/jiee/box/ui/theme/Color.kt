package com.jiee.box.ui.theme

import androidx.compose.ui.graphics.Color

// Palette pulled from the JIEE BOX app icon artwork: warm cream/terracotta/blue,
// on a slightly dark warm background for readability — dynamic without being
// neon/aggressive.
val JieeBlue = Color(0xFF7FA6E8)          // primary: buttons, links (brightened for dark bg)
val JieeBlueDeep = Color(0xFF4A78C4)      // pressed/emphasis states
val JieeTerracotta = Color(0xFFF0824E)    // active-state accent
val JieeTerracottaDeep = Color(0xFFD9673A) // stop button, stronger warm accent
val JieePink = Color(0xFFFA9088)          // secondary highlights

val JieeBackground = Color(0xFF241C17)    // warm dark brown, not cold navy
val JieeSurface = Color(0xFF2E2620)       // card surfaces, slightly lighter warm dark
val JieeOutline = Color(0xFF3D332B)       // soft warm outline/border

val JieeTextPrimary = Color(0xFFF3E9DD)   // warm off-white, readable on dark
val JieeTextSecondary = Color(0xFFB7A48F) // warm muted tan for secondary text
